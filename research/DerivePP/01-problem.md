# 01. 문제 — 왜 이 개발을 했는가

## 1.1 하드웨어가 준 제약

### 보유 inventory

라즈베리파이 5 아홉 대(4코어 동일 CPU). RAM 만 다르다.

| 노드 | RAM | 스위치 |
|---|---|---|
| `.105` `.113` `.166` `.191` | 16 GB | 하위 스위치 A |
| `.94` `.96` `.103` `.104` `.54` | 8 GB | 하위 스위치 B |

### 최종 평가에 사용한 구성 (N=8)

| 역할 | 노드 | RAM |
|---|---|---|
| **root (stage 0)** | `.105` | 16 GB |
| worker | `.113` `.191` | 16 GB |
| worker | `.103` `.94` `.104` `.54` `.96` | 8 GB |

**제외: `.166` (16 GB).** 재설치 후 공개키 인증이 복구되지 않아 최종 평가에서
빠졌고, 대신 8 GB 인 `.96` 이 들어갔다. 따라서 최종 구성은 16 GB 3대 + 8 GB 5대다.

### 토폴로지 — 병목은 두 스위치 사이다

```
       ┌──────────────── 메인 스위치 ────────────────┐
       │                                            │
   업링크 100 Mb                               업링크 100 Mb
       │                                            │
 ┌─ 하위 스위치 A ─┐                        ┌─ 하위 스위치 B ─┐
 │  .105 (root)   │                        │  .103  .94     │
 │  .113  .191    │  16 GB 군               │  .104  .54     │  8 GB 군
 │  [.166 제외]   │                        │  .96           │
 └────────────────┘                        └────────────────┘
    군 내부 115.7 MB/s                        군 내부 115.5 MB/s
              └────── 군 간 11.7 MB/s ──────┘
```

### 링크 실측

`D(x) = α + x/β` 로 적합했다(`prefill_bench/bench_link.cpp`, 64 B~4 MB 왕복).

| 경로 | α (ms) | β (MB/s) |
|---|---|---|
| 16 GB 군 내부 | 0.083~0.085 | **115.7~117.0** |
| 8 GB 군 내부 | 0.084~0.087 | **115.5~116.9** |
| **군 간** | 0.126 | **11.7** |

**RAM 이 아니라 토폴로지 문제다.** 모든 노드가 `1000Mb/full` 로 협상돼 있고
8 GB 노드끼리도 115.6 MB/s 가 나온다. 두 하위 스위치를 메인 스위치에 잇는
**업링크가 100 Mb** 인 것이 원인이고, RAM 용량과의 상관은 배치의 부산물이다.

즉 **링크가 10배 비대칭**이다. 이것이 이 연구의 출발 조건이다.

> `iperf` 같은 대용량 스트리밍으로는 `α` 를 잡을 수 없고, ssh 왕복은 프로토콜
> 처리 시간이 섞여 무효다. 전용 echo 서버/클라이언트가 필요했다.

## 1.1b 프롬프트 길이 표기 규약

문서 전체에서 다음 세 값을 구분한다. 섞으면 artifact 와 충돌한다.

| 기호 | 뜻 |
|---|---|
| `S_real` | tokenizer 가 실제로 낸 토큰 수. **논문의 `S` 는 이 값이다** |
| `S_sched` | 4행 타일 정렬 패딩 후 스케줄된 위치 수 → [02b](02b-microbatch-size.md) §2b.3 |
| source file | 프롬프트 원본 파일명 (생성 시 목표치라 실제 토큰 수와 다르다) |

| `S_real` | `S_sched` | source file |
|---|---|---|
| **447** | 448 | `prompts_gen/prompt_512.txt` |
| **1789** | 1792 | `prompts_gen/prompt_2048.txt` |
| **7212** | 7212 | `prompts_gen/prompt_8192.txt` |

> **"prompt 512 토큰" 은 잘못된 표현이다.** 파일명일 뿐이다.

## 1.2 왜 TTFT(prefill)인가

LLM 추론은 두 단계다.

```
prefill   프롬프트 전체를 한 번에 처리해 KV 캐시를 만든다.  계산 집약(GEMM)
decode    토큰을 하나씩 만든다.                              메모리 대역 집약(GEMV)
```

**첫 토큰까지의 지연(TTFT)은 prefill 이 결정한다.** 그리고 prefill 은 배치
차원이 있어 GEMM 으로 돌아가므로, decode 와 달리 **계산을 나눌 여지가 크다.**
엣지에서 대화형 응답성을 좌우하는 것도 TTFT 다.

decode 는 이미 메모리 대역에 묶여 있어 노드를 늘려도 얻는 것이 적다.
그래서 **prefill 에 집중했다.**

## 1.3 기존 공개 구현이 왜 실패하는가

같은 클러스터, 모델 계열, Q4_0 양자화, CPU 스레드 수, 프롬프트 길이에서
**각 공개 시스템의 최선 실현 가능 구성**을 비교했다. 결과는 **분산이 오히려 손해**다.

| 시스템 | 최선 구성 | t/s | 로컬 단일 대비 |
|---|---|---|---|
| llama.cpp 로컬 | N=1 | 17.49 | 1.00× |
| llama.cpp RPC | N=2 | 7.23 | **0.41×** |
| 공식 distributed-llama | N=4 | 7.63 | **0.44×** |
| 공식 distributed-llama | N=8 | 2.08 | 0.12× |

> **"완전히 동일 조건" 이라고 쓰지 않는다.** 노드 수와 실행 역할이 다르다.
> 특히 RPC 의 `-ngl 99` 는 모든 레이어를 원격 디바이스에 올려 **root 가 계산에
> 거의 참여하지 않으므로, `N=2` 는 실질적으로 "원격 1노드 실행"** 이다.
> 본 연구가 root 를 stage 0 으로 쓰는 것과 구조가 다르다.
> 또한 RPC 는 개발사가 README 에서 proof-of-concept, fragile, insecure 로
> 규정한 상태다.

원인을 추측이 아니라 **소스로 확인**했다.

- **llama.cpp RPC** — 비동기 pipeline 경로가 소스에 없다.
  `set/get/cpy_tensor_async` 계열이 구현되지 않아 매 경계에서 블로킹 왕복이 생긴다.
  통신이 계산 뒤로 숨지 못하므로 노드를 늘릴수록 왕복 횟수만 는다.
- **공식 distributed-llama** — 100 Mb 업링크를 건너는 순간 붕괴한다.
  각 pipeline stage 경계에서 activation 을 **동기 전송**하며, 느린 업링크를
  계산과 충분히 중첩하지 못한다. 스위치 **안**에서는 확장하다가 업링크에서
  무너지므로, 링크 비대칭이 실제로 시스템을 무너뜨리는 것을 확인한 것이다.

**이것이 개발의 직접적 동기다.** "분산하면 빨라진다" 가 이 환경에서는 성립하지
않았고, 성립시키려면 **통신을 계산 뒤로 숨기는 구조**가 필요했다.

## 1.4 왜 어려운가 — 노드 사이의 두 벽

### 벽 1. 통신이 계산과 겹치지 않는다

레이어를 노드에 나누면(pipeline parallel) 경계마다 활성값을 넘겨야 한다.
순차 실행이면 `계산 → 전송 → 계산 → 전송` 이라 전송 시간이 그대로 더해진다.
100 Mb 구간에서는 이게 치명적이다.

```
B=48, d=4096 → 209 kB
  1 GbE 경계:  0.083 + 209/115.7 = 1.9 ms
  100 Mb 경계: 0.126 + 209/11.7  = 18.0 ms      ← 9.5배
```

### 벽 2. 커널 비용이 계단이다

Q4_0 repack GEMM 은 4행 단위로 돌고 나머지는 느린 gemv 로 처리한다.
그래서 배치 크기에 대한 비용이 **매끄럽지 않고 계단**이다.

```
c(15) = 327 ms  >  c(16) = 148 ms      토큰이 더 적은데 2.2배 느리다
```

파이프라인에서는 이 초과분이 **스테이지 수만큼 누적**된다. 회복되지 않는다.

## 1.5 벽 3 — 노드 안에서도 CPU 가 논다

위 두 벽은 **노드 사이** 문제다. 그런데 노드 **안**에도 같은 성격의 낭비가 있다.

Q4_0 repack GEMM 은 스레드를 **출력 열**로 나눈다. 그런데 모든 스레드가
**배치 전체의 activation** 을 필요로 한다. executor 는 op 경계에서만 동기화하고
op 내부 배리어가 없다. 그래서 **각 스레드가 activation 전체를 자기 스크래치에
중복 변환한다.**

```
스레드 0:  activation 전체 → 자기 packed copy → 자기 output column
스레드 1:  activation 전체 → 자기 packed copy → 자기 output column
스레드 2:  ...
스레드 3:  ...
```

pack 연산량 자체는 전체의 **0.30%** 에 불과하다. 문제는 **4개의 독립 cache
footprint** 가 생겨 4스레드 확장이 무너지는 것이다.

| 스레드 수 | duplicate vs shared |
|---|---|
| 1~3 | 1.00~1.03× (차이 없음) |
| **4** | **1.68×** |

## 1.6 두 계층은 같은 병이다

```
노드 사이:  데이터를 넘길 때마다 기다린다        → 통신이 계산과 안 겹친다
노드 안:    소비자마다 데이터를 다시 준비한다     → cache 를 중복 점유한다
```

공통 원리로 묶인다.

> **데이터를 소비자마다 반복 준비하지 않고, 적절한 계층에서 한 번 materialize 한 뒤
> 파이프라인으로 전달하거나 공유한다.**

## 1.7 그래서 무엇을 만들었나

| 계층 | 벽 | 대응 | 문서 |
|---|---|---|---|
| 노드 간 | 통신이 계산과 안 겹침 | **Wave Pipeline** | [02](02-wave-pipeline.md) |
| 노드 간 | 계단 비용 함수 | `B` 를 wave 실행 파라미터로 선택 | [02b](02b-microbatch-size.md) |
| 노드 내 | 중복 packing 의 cache cliff | **SharedPack** | [03](03-sharedpack.md) |
| 결합 | — | 계층 co-design | [04](04-codesign.md) |

## 1.8 스스로에게 건 제약

측정과 일반성을 지키기 위해 처음부터 금지한 것들이다.

| 금지 | 이유 |
|---|---|
| 특정 weight 값이나 근사 연산에 의존하는 **전용 산술 커널**을 핵심 전제로 두기 | 일반성이 떨어진다. 다른 플랫폼·모델로 안 옮겨간다. (executor·graph builder·matmul 소비 경로 수정은 이에 해당하지 않는다 — SharedPack 이 그 예다) |
| 양자화 변경 | 정확도를 대가로 지불한다. 공정 비교가 깨진다 |
| 모델 재로드 기반 재분할 | 측정에 로드 시간이 섞인다 |
| 런타임 레이어 마이그레이션 | KV 일관성이 깨진다 |
| 온도 기반 접근 | 재현되지 않는다 |
| Tailscale(100.x) 사용 | WireGuard 오버레이가 통신 측정을 오염시킨다. **공인/LAN IP 만** |
