# generality — SharedPack 일반성 스윕

`S_real {447, 1789, 7212} × B {16, 32} × threads {1, 4}`, N=8 wave on.
조합마다 `A,B,B,A` 를 **연달아** 돌려 같은 세션 안에서만 비율을 만든다.

harness: `prefill_bench/generality.sh`

## 파일

```
run.tsv       1차 자료. 완료된 회차만 한 줄씩 추가된다
raw/*.log     각 회차의 전체 로그
raw/*.bin     프리필 종료 로짓 f32 dump (.gitignore 대상)
```

## 0바이트 로그는 실패가 아니다

셸 리다이렉트로 파일을 먼저 만들고 실행이 끝난 뒤 채워진다.
따라서 **진행 중인 회차의 로그는 0바이트다.**

```
0바이트 + run.tsv 에 해당 tag 없음   → 진행 중
0바이트 + run.tsv 에 tag 있음        → 실제 실패. 조사 대상
```

`run.tsv` 에 줄이 있는데 `prefillMs` 가 `FAIL` 이면 timeout 또는 비정상 종료다.

## 진행률을 문서 본문에 적지 않는다

`run.tsv` 의 줄 수가 1차 자료다. 본문에 "27/48" 같이 적으면 곧 낡는다.

```
완료 회차 수:  tail -n +2 run.tsv | wc -l
전체 회차 수:  3 x 2 x 2 x 4 = 48  (+ 웜업 1, 폐기)
```

## 최종 결과 (48/48 완료, 바이너리 `a6c22236`)

```
조합                  BASE         SP        R     sw비
s447_B16_t4         6523.5     4400.9   1.4823   2.107
s447_B32_t4         7718.9     5257.6   1.4682   1.822
s1789_B16_t4       25678.5    18643.6   1.3773   2.261
s1789_B32_t4       30854.4    23249.5   1.3271   1.756
s7212_B16_t4      159801.1   130411.0   1.2254   2.576
s7212_B32_t4      210134.6   179660.8   1.1696   1.354

s447_B16_t1        13588.3    13619.5   0.9977   0.976
s447_B32_t1        16512.3    16419.9   1.0056   1.008
s1789_B16_t1       59715.9    59613.4   1.0017   1.025
s1789_B32_t1       66597.3    67339.8   0.9890   0.961
s7212_B16_t1      443674.6   441520.3   1.0049   1.111
s7212_B32_t1      474950.9   469810.6   1.0109   1.023

4스레드 6조합   범위 1.1696~1.4823   기하평균 1.3366
1스레드 6조합   범위 0.9890~1.0109   기하평균 1.0016
```

## `s7212_B32_t1` 은 재수행분이다

1차 시도에서 3회가 실패했다.

```
tryReadSocket timeout after 60000ms
Failed to drain prefill logits from last PP stage
```

`S=7212, B=32, 1스레드` 에서 마지막 스테이지의 마이크로배치 하나가 60초
기본 타임아웃(`DEFAULT_NET_STALL_TIMEOUT_MS`)을 넘겼다. **코드 결함이 아니라
harness 기본값 한계다.**

`DLLAMA_NET_STALL_TIMEOUT_MS=600000` 으로 올려 **같은 바이너리 `a6c22236`** 으로
재수행해 채웠다. 재수행 원본은 `refill.tsv` 에 보존한다.

> 사전등록한 설계에 구멍을 남기지 않기 위해 다시 돌렸다.
> "1스레드라 정보가 적으니 건너뛴다" 는 사후 판단이다.

## 로짓 해시

같은 `S_real` 안에서 BASE 와 SP 의 md5 가 같아야 한다.
길이가 다르면 해시도 다르다 — 서로 비교하지 않는다.

```
S_real=447    c52e50e37e65
S_real=1789   acd54508ab4f
S_real=7212   277ff4dd3e36
```

12개 조합 전부에서 BASE 와 SP 의 해시가 같다.
